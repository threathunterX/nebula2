# 安全策略

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。**

请发送邮件至 **security@threathunter.cn**,包含:

- 漏洞类型与影响范围
- 复现步骤或 PoC
- 你认为的严重程度
- 是否已在公开渠道披露

我们会在 **3 个工作日内**确认收到,并在 **14 天内**给出初步评估。修复发布后会在 release notes 中致谢报告者(如你希望匿名请注明)。

## 支持的版本

| 版本 | 状态 |
|---|---|
| 2.x | 🚧 开发中,尚未发布 |
| 1.x | ❌ **已停止维护**,不再接受安全修复 |

> **关于 1.x 的重要提示**:[Nebula 1.x](https://github.com/threathunterX/nebula) 于 2019 年后停止维护,其依赖的 Python 2、OpenResty 1.11、Esper 6、jackson 1.9、commons-collections 3.2.1 等组件均存在已知漏洞(其中 commons-collections 3.2.1 含经典反序列化利用链)。**请勿在生产环境部署 1.x。**

## 本项目的安全设计

2.0 在设计阶段就纳入了以下要求,详见 [docs/security/](docs/security/):

**凭据管理**
- 零默认口令。首次启动生成随机凭据,或强制从环境注入;代码中不存在任何可用凭据的 fallback
- 全部配置外置,支持环境变量、文件、Kubernetes Secret 与 Vault
- CI 强制 secret scanning(gitleaks),发现疑似凭据即阻断合并

**认证与授权**
- 用户口令使用 **Argon2id**(1.x 为无盐单次 SHA1)
- 服务间调用使用短期 JWT 或 mTLS(1.x 使用 5 个硬编码的免登录 token,且鉴权逻辑为「来源 IP 白名单 **或** token 匹配」,拿到 token 即可从任意来源冒充内部身份)
- 会话密钥启动时生成并支持轮换(1.x 硬编码 cookie_secret,可离线伪造任意用户会话)
- 支持 OIDC 对接企业身份源
- 全部管理操作写入审计日志

**供应链**
- 每次构建生成 SBOM
- 依赖与镜像扫描(Trivy / Grype)接入 CI
- 依赖版本锁定,定期自动升级

**隐私**
- 敏感字段在采集端就地脱敏,原文不进入下游
- 个人标识符以 HMAC 或加密列存储
- 数据保留期由存储层 TTL 自动执行
- 提供数据主体的导出与删除接口

## 部署方的责任

星云处理的是**业务流量**,其中必然包含个人信息。部署前请确认:

1. 已按业务实际情况配置字段脱敏规则(默认规则从严,但无法预知你的自定义字段)
2. 已设置符合当地法规的数据保留期
3. 已限制控制台的网络暴露面,并配置了强认证
4. 已评估合规义务(中国大陆:个人信息保护法、数据安全法、网络安全等级保护)

详见[隐私设计与合规](docs/security/privacy.md)。
