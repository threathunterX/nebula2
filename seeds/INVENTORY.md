# Nebula 2.0 Seeds — INVENTORY

Generated by `tools/extract_seeds.py` from the legacy Nebula database bootstrap dump (`scripts/db/nebula.init.data.sql`). The source file was read only; nothing in it was modified.

## 1. Totals

| Asset | Count | Output | Source table |
|---|---|---|---|
| Event models | 17 | `events/*.json` | `nebula_default.eventmodel_default` |
| Variable models | 253 | `variables/*.json` | `nebula_default.variablemodel_default` |
| Strategy templates | 170 | `strategies/*.json` | `nebula.strategy_cust` |
| Risk tags | 15 | `tags.json` | `nebula.tags` |
| Config defaults | 33 | `config-defaults.json` | `nebula_default.config_default` |

## 2. Event models (17)

| name | visible_name | type | version | # properties |
|---|---|---|---|---|
| ACCOUNT_CERTIFICATION | 账号-实名验证 | base | 1.0 | 4 |
| ACCOUNT_LOGIN | 账号-登录 | base | 1.0 | 7 |
| ACCOUNT_PW_CHANGE | 账号-密码修改 | base | 1.0 | 7 |
| ACCOUNT_REFERRALCODE_CREATE | 账号-生成推荐码 | base | 1.0 | 3 |
| ACCOUNT_REGISTRATION | 账号-注册 | base | 1.0 | 9 |
| ACCOUNT_TOKEN_CHANGE | 账号-安全凭证修改 | base | 1.0 | 6 |
| ACTIVITY_DO | 营销-参加活动 | base | 1.0 | 8 |
| HTTP_DYNAMIC | 动态资源请求 | base | 1.0 | 30 |
| HTTP_DYNAMIC_DELAY | http动态资源访问 | base | 1.0 | 1 |
| HTTP_STATIC | 静态资源请求 | base | 1.0 | 30 |
| ORDER_CANCEL | 订单-取消 | base | 1.0 | 6 |
| ORDER_SUBMIT | 订单-提交 | base | 1.0 | 30 |
| TRANSACTION_BANKCRD_BIND | 支付-绑卡 | base | 1.0 | 8 |
| TRANSACTION_BANKCRD_UNBIND | 支付-解绑 | base | 1.0 | 6 |
| TRANSACTION_DEPOSIT | 支付-充值 | base | 1.0 | 7 |
| TRANSACTION_ESCROW | 支付-第三方支付 | base | 1.0 | 14 |
| TRANSACTION_WITHDRAW | 支付-取现 | base | 1.0 | 8 |

Total property definitions across all event models: **184**.

## 3. Variable models (253)

### By `module`

| module | count |
|---|---|
| slot | 98 |
| realtime | 97 |
| profile | 39 |
| base | 19 |

### By `dimension`

| dimension | count |
|---|---|
| uid | 89 |
| did | 58 |
| ip | 58 |
| (empty) | 19 |
| global | 18 |
| page | 11 |

### By `type`

| type | count |
|---|---|
| aggregate | 172 |
| top | 34 |
| dual | 25 |
| event | 17 |
| sequence | 3 |
| filter | 2 |

### By `status`

| status | count |
|---|---|
| enable | 252 |
| (empty) | 1 |

### By `value_type`

| value_type | count |
|---|---|
| long | 80 |
| map | 57 |
| mmap | 52 |
| double | 34 |
| (empty) | 19 |
| string | 7 |
| list | 3 |
| mlist | 1 |

## 4. Strategy templates (170)

### By `category`

| category | count |
|---|---|
| ORDER | 70 |
| ACCOUNT | 60 |
| VISITOR | 40 |

### By `tag`

| tag | count |
|---|---|
| 单一下单 | 24 |
| 不同下单 | 21 |
| 高频登录 | 17 |
| 高频访问 | 12 |
| 高频注册 | 11 |
| 高频关联 | 8 |
| 跳跃访问 | 7 |
| 关联下单 | 6 |
| 关联注册 | 6 |
| 关联登录 | 6 |
| 恶意扫描 | 6 |
| 高频下单 | 6 |
| 特殊下单 | 5 |
| SQL注入 | 4 |
| 单一访问 | 4 |
| 特殊UA | 4 |
| 邀请注册 | 4 |
| XSS | 3 |
| 下单不支付 | 3 |
| 取消订单 | 3 |
| RFI | 2 |
| ngx_lua_waf | 2 |
| 午夜下单 | 2 |
| 目录遍历 | 2 |
| 一天关联 | 1 |

### By `status`

| status | count |
|---|---|
| online | 170 |

Total strategy terms (conditions/actions) across all templates: **607** (avg 3.6 per strategy).

## 5. Risk tags (15)

| tag | app |
|---|---|
| RFI | nebula |
| SQL注入 | nebula |
| XSS | nebula |
| ngx_lua_waf | nebula |
| 其他 | nebula |
| 刷单 | nebula |
| 恶意扫描 | nebula |
| 恶意注册 | nebula |
| 撞库 | nebula |
| 爬虫 | nebula |
| 盗号 | nebula |
| 目录遍历 | nebula |
| 简历爬虫 | nebula |
| 羊毛党 | nebula |
| 账号异常 | nebula |

## 6. Sanitization changes

| Original (masked) | Replacement | Occurrences | Why |
|---|---|---|---|
| `<username>@<legacy-corp-domain>` | `alerts@example.com` | 1 | Real employee mailbox at a real company domain, used as the alert-email sender. |
| `nebula.<legacy-corp-domain>` | `nebula.example.com` | 1 | Real customer corporate hostname (the Nebula console base URL embedded in alert emails). |
| `<name>@<throwaway-domain-2>` | `alerts-secondary@example.net` | 1 | Looks like a dummy, but the domain is a real registered domain; moved into the RFC 2606 example space. |
| `<name>@<throwaway-domain-1>` | `alerts-primary@example.com` | 1 | Looks like a dummy, but the domain is a real registered domain; moved into the RFC 2606 example space. |

> The originals are described rather than quoted so this report can ship alongside the seeds. The literal search patterns live in `tools/sanitize_rules.json` — that file is the only place the real identifiers appear, and it should be removed or git-ignored before the seed set is published.

### Scanned for, none found in the extracted assets

- Mainland-China mobile numbers (`1[3-9]xxxxxxxxx`)
- Resident ID numbers (18-digit)
- Bank / payment card numbers (UnionPay `62…`, Visa `4…`, Mastercard `5[1-5]…`)
- Public (non-RFC1918 / non-RFC5737) IPv4 literals
- Real personal names

Field names such as `phone`, `id_card`, `bank_card`, `uid` and `c_ip` do occur in event/variable **schemas** — those are metadata definitions (column descriptors), not customer values, and are kept intentionally.

### Residual scan of the emitted JSON

Clean — no email addresses outside the `example.*` space, no phone numbers, ID numbers, card numbers, or public IP literals remain.

## 7. Placeholders

| Placeholder | Legacy token | Occurrences |
|---|---|---|
| `<YOUR_PAYMENT_PAGE_PATH>` | HOLDER | 7 |

See `PLACEHOLDERS.md` for what each one means and how to fill it in.

## 8. Output conventions

- UTF-8, `ensure_ascii=False` (CJK stays readable), 2-space indent, `sort_keys=True`, trailing newline — byte-stable and diff-friendly.
- Columns that held serialized JSON in MySQL (`properties`, `source`, `filter`, `period`, `function`, `groupbykeys`, `hint`, `config`) are parsed into real JSON structures rather than left as escaped strings.
- Volatile bookkeeping columns (`id`, `last_modified`, `createtime`, `modifytime`) are dropped from the per-asset files so that re-running the extraction produces identical output.

---

## 迁移到 2.0 时修正的 1.x 数据缺陷

以下缺陷由 `tools/validate_seeds.py` 的 schema 校验发现。它们存在于 1.x 的出厂配置中,随发行版一同分发了多年。

| 变量 / 事件 | 字段 | 1.x 原值 | 修正为 | 说明 |
|---|---|---|---|---|
| `TRANSACTION_ESCROW` | `status` | `""` | `enable` | 其余 16 个内置事件均为 `enable`,此处为空值 |
| `uid__account_login_ip_last__profile` | `app` | `""` | `nebula` | 命名空间字段为空 |
| `uid__account_login_distinct_did__1h_profile` | `name` | `..._1h_profile` | `..._1h__profile` | 命名规范要求分段用双下划线,此处少一个下划线。同步更新了引用该变量的 `uid__account_login_distinct_count_did_succ__1d__profile` |

另有一处是 2.0 schema 自身的疏漏,已补正:`last_value` 算子(1.x `SIMPLE_CALCULATOR_MAP` 中的真实算子,用于取 map 型变量的最新值)最初未纳入 schema 的算子枚举。

> 这四处问题都是在 CI 校验中被自动发现的,而非人工审阅。这正是 [ADR-0005](../docs/adr/0005-schema-single-source-of-truth.md) 采用「schema 作为单一真相源 + 强制校验」的意义:1.x 的元数据没有任何机器校验,这类缺陷只能靠运行时报错或人工发现。

---

## 隐私标注(2.0 新增)

1.x 的变量定义中没有任何敏感级别信息。迁移到 2.0 时,对全部 253 个变量的**值**做了敏感性评估,结果:

| 级别 | 数量 | 含义 |
|---|---|---|
| `pii` | 20 | 值(或 map 的键)是个人标识,要求 HMAC 存储 |
| `internal` | 7 | 由个人信息派生但本身不可识别到个人(计数、时间戳、金额) |
| 未标注(按 `internal` 处理) | 226 | 纯统计量 |

20 个 `pii` 变量**全部位于 `profile` 模块**——保留期最长的一层(默认 180 天)。这不是巧合:长期画像的价值来自保留可识别的历史,风险也集中在此。

其中最容易被低估的是「关联图谱」一类:`uid__visit_distinct_did__1h__profile` 这类变量单看定义像纯统计量,但值是设备标识的集合,与账号维度交叉后可以还原出"谁在什么设备上用什么网络访问过"。

评估依据与逐条理由见[隐私设计](../docs/security/privacy.md),完整清单见[变量参考](../docs/reference/variables.md)。

**机制保障**:`tools/validate_seeds.py` 强制要求 `profile` 模块中值为可读类型的变量必须显式声明 `sensitivity`,新增变量时无法跳过评估。这道检查在首次引入时就逼出了 13 个此前未评估的变量。
