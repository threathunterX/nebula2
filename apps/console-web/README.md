# console-web —— 管理界面

🚧 实现中。

React 19 + TypeScript + Vite,图表使用 ECharts 5。

替代 1.x 的 `nebula_fe`(React 15 + webpack 3)与自研图表库 `BChart`(基于 D3 v3 的 5600 行单文件)。类型定义由 `packages/domain-schema` 生成,策略编辑器的表单校验规则同样由 schema 派生。
